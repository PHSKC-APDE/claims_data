% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper_functions.R
\name{suppress_f}
\alias{suppress_f}
\title{Apply suppression to data frame (total result set or column-wise)}
\usage{
suppress_f(df, suppress_var, lower = 1, upper = 10, col_wise = TRUE)
}
\arguments{
\item{df}{Data frame on which to perform small number suppression}

\item{suppress_var}{Character vector specifying which variables to base suppression on.
These should be numeric columns containing count data.}

\item{lower}{Lower bound for suppression range (inclusive). Defaults to 1.}

\item{upper}{Upper cell count for suppression (inclusive). Values less than or 
equal to this number will be considered for suppression. Defaults to 10.}

\item{col_wise}{Logical. Whether to perform column-wise suppression (`TRUE`) or 
total result set suppression (`FALSE`). Defaults to `TRUE`.}
}
\value{
A data frame with suppressed values replaced by `NA`
}
\description{
Applies suppression to data frame by replacing values within 
specified ranges with NA. Can perform either column-wise suppression 
(suppress individual cells) or total result set suppression (suppress entire 
rows when any suppression variable meets criteria).
}
\details{
Column-wise suppression replaces individual cells that fall within 
the suppression range, while total result set suppression removes entire rows 
when any of the specified suppression variables meet the criteria.
}
\examples{
\dontrun{
# Create sample data
sample_data <- data.frame(
  category = c("A", "B", "C"),
  member_count = c(5, 15, 8),
  claim_count = c(3, 25, 12)
)

# Apply column-wise suppression to count variables
suppressed_data <- suppress_f(
  df = sample_data, 
  suppress_var = c("member_count", "claim_count"),
  lower = 1, 
  upper = 10
)

# Apply total result set suppression
suppressed_data <- suppress_f(
  df = sample_data,
  suppress_var = "member_count", 
  col_wise = FALSE
)
}

}
\keyword{data}
\keyword{privacy}
\keyword{utilities}
