schema: stg_claims
table: stage_mcare_claim_provider
vars:
    id_mcare: varchar(255) collate SQL_Latin1_General_Cp1_CS_AS
    claim_header_id: varchar(255) collate SQL_Latin1_General_Cp1_CS_AS
    first_service_date: date
    last_service_date: date
    provider_npi: bigint
    provider_type: varchar(255)
    provider_type_nch: varchar(255)
    provider_tin: varchar(255)
    provider_zip: varchar(255)
    provider_specialty: varchar(255)
    filetype_mcare: varchar(255)
    last_run: datetime
